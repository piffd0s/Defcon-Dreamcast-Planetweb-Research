diff --git a/doomgeneric/i_system.c b/doomgeneric/i_system.c
index 53ab2c9..9070e63 100644
--- a/doomgeneric/i_system.c
+++ b/doomgeneric/i_system.c
@@ -55,8 +55,8 @@
 #include <CoreFoundation/CFUserNotification.h>
 #endif
 
-#define DEFAULT_RAM 6 /* MiB */
-#define MIN_RAM     6  /* MiB */
+#define DEFAULT_RAM 3 /* MiB */
+#define MIN_RAM     2  /* MiB */
 
 
 typedef struct atexit_listentry_s atexit_listentry_t;
diff --git a/doomgeneric/s_sound.c b/doomgeneric/s_sound.c
index f6d8be1..cf1a3d7 100644
--- a/doomgeneric/s_sound.c
+++ b/doomgeneric/s_sound.c
@@ -396,6 +396,7 @@ void S_StartSound(void *origin_p, int sfx_id)
     int sep;
     int cnum;
     int volume;
+    return; /* DC: no audio -> skip SFX (avoids DS* lump lookup so they can be trimmed) */
 
     origin = (mobj_t *) origin_p;
     volume = snd_SfxVolume;
@@ -603,6 +604,7 @@ void S_ChangeMusic(int musicnum, int looping)
     musicinfo_t *music = NULL;
     char namebuf[9];
     void *handle;
+    return; /* DC: no audio -> skip music (avoids D_* lump lookup so they can be trimmed) */
 
     // The Doom IWAD file has two versions of the intro music: d_intro
     // and d_introa.  The latter is used for OPL playback.
